<Window x:Class="ToDo.Views.Dialogs.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:ToDo.Services"
        Title="{x:Static services:Loc.UpdateAvailable}" Height="380" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource CardBackgroundBrush}"
        FontFamily="{StaticResource FluentFont}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="VersionText" Grid.Row="0" FontSize="18" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
            <TextBlock x:Name="BodyText" TextWrapping="Wrap" FontSize="13"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
        </ScrollViewer>

        <ProgressBar x:Name="Progress" Grid.Row="2" Height="6" Visibility="Collapsed"
                     Margin="0,0,0,12" Maximum="100"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static services:Loc.SkipVersion}" Style="{StaticResource FluentSubtleButton}"
                    FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,8,0"
                    Click="Skip_Click"/>
            <Button Content="{x:Static services:Loc.RemindLater}" Style="{StaticResource FluentButton}"
                    FontSize="13" Margin="0,0,8,0" Click="RemindLater_Click"/>
            <Button x:Name="DownloadBtn" Content="{x:Static services:Loc.DownloadUpdate}"
                    Style="{StaticResource FluentAccentButton}" FontSize="13" Click="Download_Click"/>
        </StackPanel>
    </Grid>
</Window>
