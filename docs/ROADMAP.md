# To Do — 路线图

> 长期规划文档，只记录**尚未完成**的工作；已实现的功能以 [CHANGELOG.md](../CHANGELOG.md) 为准。
> 条目按优先级排序：**P0** 下一件推进 → **P1** 近期快活（穿插 / 短平快）→ **P2** 中期 → **P3** 远期。优先级随项目状态调整。

---

## P0 — 下一件大功能

### 1. 重复任务（recurring）

目前完全没有，是与 Microsoft To Do 差距最大的功能。

- [ ] 每日 / 工作日 / 每周 / 每月等重复规则
- [ ] 完成一次后按规则生成下一次；与提醒、同步模型结合
- [ ] 覆盖重复提醒场景
- [ ] 工程上最重：涉及数据模型与同步语义（LWW / 墓碑 / 下一实例生成时机），建议先写 ADR 定方案再动手

## P1 — 近期（短平快，穿插完成）

### 2. 工程：测试覆盖可度量

当前测试约 150 个，但**无覆盖率度量、盲区不透明**：服务端仅 API 5 + Merge 9；客户端 code-behind（MainWindow.xaml.cs 等）大量 UI 逻辑无测试。

- [ ] 接入 coverlet 输出覆盖率报告，先看清盲区
- [ ] 补齐服务端同步 / 合并关键路径测试
- [ ] 按覆盖率结果补客户端关键路径测试

> 本地跑客户端测试会被运行中的 app 锁 `ToDo.Core.dll`（MSBuild 报 MSB3027），需先关闭运行实例；CI 无此问题。

### 3. 工程：CI 增加 Release 构建

ci.yml 目前只跑 Debug；Release 发布路径只在打 tag 时验证，回归要到发布阶段才发现。

- [ ] 常规门禁中增加 `-c Release` 构建，让发布路径在 CI 内即被验证

### 4. 工程：拆分单体文件 ✅

已完成：MainViewModel.cs → 4 个 partial（主题 / 列表 / 任务 / 标签）；MainWindow.xaml.cs（2117 行）→ 7 个 partial → 100 行；MainWindow.xaml（1509 行）→ 抽 SidebarControl / TaskListControl / DetailPaneControl 三个 UserControl，骨架缩至 86 行，6 个已清空的 partial 已删除。

- [x] MainViewModel / MainWindow 代码后置拆分
- [x] MainWindow.xaml 拆分为三个 UserControl

### 5. 撤销完成 / 删除

删除是永久性的，无撤销。

- [ ] 完成 / 删除操作后出现撤销入口（如 MS To Do 的 toast 撤销）

## P2 — 中期

### 6. 提醒交互增强

- [ ] ReminderToast 增加操作按钮：稍后提醒 / 打开任务 / 完成
- [ ] 应用未运行、到点未触发时的补发策略（当前启动时仅静默标记，不补弹）
- [ ] 可选：改用 Windows 原生 toast notification（现为应用内 Fluent 卡片）

### 7. 提醒跨设备一致性

提醒去重键（`_fired`）是内存态、不参与同步；多设备场景下提醒触发 / 抑制可能不一致。多端用户才会遇到的正确性问题。

- [ ] 评估提醒状态持久化或进入同步模型

### 8. 键盘快捷键

目前全局仅 Ctrl+N / Esc，其余 Enter/Esc 多为行内编辑确认。

- [ ] 常用操作快捷键：新建任务 / 搜索 / 列表切换 / 完成任务等，完善键盘可达性

### 9. 本地化管线

当前「静态字符串 + 窗口重建」，能用但加语言成本高（每处文案都要手改源码 + 重建）。

- [ ] 评估 RESX / 资源管线，让新增语言可维护

## P3 — 远期

### 10. 自定义铃声完善

铃声目前仅支持单个 wav 文件（基于 `SoundPlayer`）。

- [ ] 支持 mp3 / m4a / ogg 等格式（需引入解码或播放库）
- [ ] 内置铃声库（多首可选）
- [ ] 音量控制 / 试听时长

### 11. MAUI 安卓端

ADR-010 已规划；`ToDo.Core` 按可复用设计、同步协议带版本号，移动端可直接复用。

- [ ] 评估并启动移动端客户端（工程量最大，建议放在其余各项之后）
