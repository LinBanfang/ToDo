node_modules/
bin/
obj/
*.db
.vs/

# Claude Code local (personal) settings — not shared
.claude/settings.json
.claude/settings.local.json

# Visual Studio per-user files
*.csproj.user

# Deploy local config (real server IP/domain) — never committed
ToDo.Server/deploy/deploy.local

# Test / coverage artifacts (dotnet test --collect + reportgenerator output)
TestResults/
coveragereport/
